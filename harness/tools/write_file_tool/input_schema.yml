properties:
  file_path:
    type: string
    description: Absolute path of the file to create or overwrite.
  content:
    type: string
    description: Full UTF-8 contents to write. No trailing newline is added implicitly.
required:
  - file_path
  - content
